# ===================================================================
# Bengaluru Air Quality Monitoring System - Environment Configuration
# ===================================================================

# 🌤️ OpenWeather API (REQUIRED)
# Get your free API key from: https://openweathermap.org/api
# Free tier includes: 1000 calls/day, air pollution data, weather data
OPENWEATHER_API_KEY=your_openweather_api_key_here

# 🤖 OpenAI API (Optional - for AI chatbot features)
# Get your API key from: https://platform.openai.com/api-keys
# Used for: Natural language processing, voice assistant, smart recommendations
OPENAI_API_KEY=your_openai_api_key_here

# 🖥️ Server Configuration
# Port for the application server (default: 5000)
PORT=5000

# Environment mode (development or production)
NODE_ENV=development

# 💾 Database Configuration (Optional)
# Default: SQLite with better-sqlite3 (./data/air_quality.db)
# For PostgreSQL, uncomment and configure the line below:
# DATABASE_URL=postgresql://username:password@localhost:5432/airquality

# 🔐 Session Configuration (Optional)
# Secret key for session encryption - change this in production!
SESSION_SECRET=your_secure_session_secret_here

# 📊 Data Collection Configuration (Optional)
# How often to collect data from OpenWeather API (in milliseconds)
# Default: 900000 (15 minutes)
# DATA_COLLECTION_INTERVAL=900000

# 🔔 Notification Configuration (Optional)
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# NOTIFICATION_EMAIL=alerts@yourdomain.com

# 📡 WebSocket Configuration (Optional)
# WS_PORT=5000
# WS_PATH=/ws

# ===================================================================
# IMPORTANT NOTES:
# ===================================================================
# 1. Never commit this file with real API keys to version control
# 2. OpenWeather API key takes 10-20 minutes to activate after creation
# 3. The system will use mock data if no API key is provided
# 4. OpenAI API key is optional - system has fallback responses
# 5. For production deployment, use strong SESSION_SECRET
# ===================================================================
